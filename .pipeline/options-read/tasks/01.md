---
id: "01"
title: "options-read — omi option-chain: conid resolve + End-bounded reqSecDefOptParams drain (ADR 0019)"
status: todo
attempts: 0
verify:
  - "cargo build"
  - "cargo test --test option_chain_command"
spec-paths:
  - "tests/option_chain_command.rs"
impl-paths:
  - "src/cli.rs"
  - "src/ib/option_chain.rs"
  - "src/ib/mod.rs"
  - "src/main.rs"
  - "AGENTS.md"
  - "CLAUDE.md"
spec-rev: "b2eb7fa57c81e14ce6b6a568dc3c5180c0c849c4"
---

# Card 01 — option-chain (options-read, READ-ONLY)

Make `tests/option_chain_command.rs` green: `omi option-chain SYMBOL [--exchange SMART]` per
**ADR 0019**. Read `PRD.md` + `arch.md` + ADR 0019 + `CONTEXT.md` first — arch.md §Component
design is the verbatim implementation. Independent of card 02 (PRD D8).

## Scope (impl-paths only — do NOT touch `tests/`)

- `src/cli.rs`: `OptionChain(OptionChainArgs)` variant (clap kebab-cases to `option-chain`);
  `OptionChainArgs` verbatim from arch.md §CLI (positional `symbol`, `--exchange` default
  `"SMART"`). Doc-comment: "Option chain (expirations × strikes) for an underlying".
- `src/ib/option_chain.rs` (NEW):
  - `pub struct ChainRow { pub exchange: String, pub trading_class: String,
    pub multiplier: String, pub expirations: Vec<String>, pub strikes: Vec<f64> }` —
    ibapi-free (frozen tests construct it).
  - `pub fn shape_option_chain(underlying: &str, conid: i32, rows: Vec<ChainRow>) -> Value` —
    pure FROZEN seam: sort each row's expirations (lexicographic) + strikes (partial_cmp)
    ascending, rows by (exchange, trading_class); envelope
    `{underlying, conid, chains: [...]}`; zero rows ⇒ `chains: []` success.
  - `pub fn option_chain(cfg: &Config, args: &OptionChainArgs) -> Result<Value, AppError>` —
    gateway fn (NOT frozen): connect → `Contract::stock(&args.symbol).build()` →
    `contract_details` → FIRST row's `contract.contract_id` (empty ⇒
    `AppError::not_found`, contract.rs mirror) →
    `client.option_chain(&args.symbol, &args.exchange, SecurityType::Stock, conid)` →
    timeout-wrapped drain: the completed_orders.rs:83-128 skeleton VERBATIM
    (`timeout_iter_data(super::TAKE_FIRST_TIMEOUT)`, Instant-classified None arms:
    starved ⇒ `AppError::timeout` naming reqSecDefOptParams, instant ⇒ End ⇒ success);
    map `OptionChain` → `ChainRow` field-for-field; error context `"option-chain"`.
- `src/ib/mod.rs`: `mod option_chain;` +
  `pub use option_chain::{option_chain, shape_option_chain, ChainRow};`
- `src/main.rs`: one dispatch arm.
- `AGENTS.md` + `CLAUDE.md`: the TWO amendments from arch.md §Docs amendment VERBATIM
  (red-line sentence in both files; stale "Read-only (no order-placement code)." line in
  AGENTS.md §What this is) — no other change to those files.

## Hard constraints

- **Freeze gate**: `git diff b2eb7fa5 <tip> -- tests/option_chain_command.rs` EMPTY. Never
  touch `tests/`; never repo-wide `cargo fmt`.
- READ-ONLY: no write calls anywhere (`place_order`/`cancel_order` etc. stay confined to
  `trade.rs`, untouched); normal review polarity.
- `quote.rs` untouchable (ADR 0013 byte-identity).
- Reuse `TAKE_FIRST_TIMEOUT` — no new consts, no new error codes.
- Public repo: no account ids/tokens/balances.
- `verify` card-scoped; before PR: full `cargo test` red ONLY on card 02's target (until its
  impl lands) + clippy `-D warnings` clean.

## Freeze coverage

- **Frozen (`tests/option_chain_command.rs`, 8 tests, offline)**: shape_option_chain sorting
  (expirations/strikes ascending, rows by (exchange, trading_class)), exact envelope + 5-key
  row, empty ⇒ `chains: []`; `--help` lists `option-chain`; subcommand help; missing symbol ⇒
  usage; dead-port ⇒ connection.
- **Review must READ**: the gateway fn (conid FIRST-row rule; timeout-wrapped End-bounded
  drain per ADR 0016 pattern; `--exchange` server-side passthrough; no writes); docs
  amendment verbatim-match vs arch.md.
- **Live (operator, paper `:4002`, PRD criterion 8)**: `omi option-chain AAPL` plausible
  expirations/strikes; SMART-filter behavior noted; Tiger `:4001` = journaled observation.
