---
id: "02"
title: "options-read — omi option-quote: option snapshot + model-only best-effort greeks (ADR 0019)"
status: in-progress
attempts: 1
verify:
  - "cargo build"
  - "cargo test --test option_quote_command"
spec-paths:
  - "tests/option_quote_command.rs"
impl-paths:
  - "src/cli.rs"
  - "src/ib/option_quote.rs"
  - "src/ib/mod.rs"
  - "src/main.rs"
spec-rev: "7c8bcaf5e9a860f17324fe2b3d5495ed984a3717"
---

# Card 02 — option-quote (options-read, READ-ONLY)

Make `tests/option_quote_command.rs` green: `omi option-quote --symbol S --expiry YYYYMMDD
--strike N --right C|P [--exchange --currency --trading-class]` per **ADR 0019**. Read
`PRD.md` + `arch.md` + ADR 0019 + `CONTEXT.md` first — arch.md §Component design is the
verbatim implementation. Independent of card 01 (PRD D8).

## Scope (impl-paths only — do NOT touch `tests/`)

- `src/cli.rs`: `OptionQuote(OptionQuoteArgs)` variant; `OptionQuoteArgs` verbatim from
  arch.md §CLI (all flags; `--exchange` default `"SMART"`, `--currency` default `"USD"`,
  `--trading-class` Option). Doc-comment: "Snapshot quote + greeks for one option contract".
- `src/ib/option_quote.rs` (NEW):
  - `pub struct GreeksRow { ... 7 × pub Option<f64>: implied_volatility, delta, gamma, vega,
    theta, option_price, underlying_price }` + `#[derive(Default)]` (frozen tests use
    `..Default::default()`); ibapi-free construction in tests.
  - `pub fn option_quote_greeks(tick: &TickTypes) -> Option<GreeksRow>` — pure FROZEN seam:
    `Some` ONLY for `TickTypes::OptionComputation` with `field` ∈
    {`TickType::ModelOption`, `TickType::DelayedModelOption`}; map the 7 value fields;
    everything else (Bid/Ask/Last/CustOptionComputation rows, price/size ticks) ⇒ `None`.
  - `pub fn shape_option_quote(symbol: &str, expiry: &str, strike: f64, right: &str,
    exchange: &str, currency: &str, trading_class: Option<&str>, delayed: bool,
    ticks: serde_json::Map<String, Value>, greeks: Option<GreeksRow>) -> Value` — pure
    FROZEN seam: 8-key `contract` echo (`multiplier` always `"100"`; `trading_class` null
    when None); `right` normalized case-insensitively to `"C"`/`"P"` (accepts c/C/call/CALL/
    p/P/put/PUT — validation upstream guarantees membership); `greeks` key present IFF
    `Some`, inside it ONLY Some-valued fields; `ticks` pass-through; `delayed` echoed.
    (Signature refined from arch.md: seam decoupled from the clap struct — the test is the
    contract.)
  - `pub fn option_quote(cfg: &Config, args: &OptionQuoteArgs) -> Result<Value, AppError>` —
    gateway fn (NOT frozen): pre-connect validation (`right` parses to Call/Put else
    `AppError::usage`; `strike > 0.0` else usage; `expiry` = 8 ASCII digits, m∈1..=12,
    d∈1..=31 else usage) → connect → `switch_market_data_type` (quote.rs:32-39 verbatim,
    same `delayed` threading) → `Contract::call(..)`/`::put(..)` `.strike(..)`
    `.expires_on(y, m, d)` `.on_exchange(..)` `.in_currency(..)` [`.trading_class(..)` when
    Some] `.build()` → `market_data(&contract).snapshot().subscribe()` → bare `iter_data()`
    to `SnapshotEnd` (quote.rs class, deliberately NOT timeout-wrapped — ADR 0019 D2):
    `quote_price_tick` hits → `ticks`; `option_quote_greeks` hits → replace greeks
    (last-model-row-wins); error context `"option-quote"`.
- `src/ib/mod.rs`: `mod option_quote;` +
  `pub use option_quote::{option_quote, option_quote_greeks, shape_option_quote, GreeksRow};`
- `src/main.rs`: one dispatch arm.

## Hard constraints

- **Freeze gate**: `git diff 7c8bcaf5 <tip> -- tests/option_quote_command.rs` EMPTY. Never
  touch `tests/`; never repo-wide `cargo fmt`.
- Validation BEFORE connect (frozen tests depend on the ordering: usage < connection).
- Greeks are BEST-EFFORT (ADR 0019 D3): absent model row ⇒ NO greeks key, success — never an
  error, no gateway greeks assertion anywhere.
- READ-ONLY; `quote.rs` untouchable (reuse `quote_price_tick` via the `super::` re-export —
  never edit the file); reuse existing error codes only.
- Public repo: no account ids/tokens/balances.
- `verify` card-scoped; before PR: full `cargo test` + clippy `-D warnings` clean (with card
  01 landed the whole suite must be GREEN).

## Freeze coverage

- **Frozen (`tests/option_quote_command.rs`, 15 tests, offline)**: option_quote_greeks
  model-row matrix (Model/DelayedModel ⇒ Some with 7 fields mapped; Bid/Ask/Last/Cust ⇒
  None; Price tick ⇒ None); shape_option_quote (8-key echo sorted, right c⇒C / put⇒P,
  multiplier "100", trading_class null/echo, delayed echo, greeks-iff + omit-None-fields,
  ticks pass-through); validation matrix (right X / strike 0 / strike -5 / expiry 7-digit /
  dashed / month-13 ⇒ usage; missing flags ⇒ usage); `--help` surface; dead-port ⇒
  connection.
- **Review must READ**: the gateway fn (validation ordering; builder chain incl.
  trading_class; bare SnapshotEnd drain — NO timeout wrap, matching quote.rs posture;
  last-model-row-wins; no writes).
- **Live (operator, paper `:4002`, PRD criterion 8)**: liquid near-month AAPL contract ⇒
  price ticks; greeks presence recorded as observation either way (absence ≠ failure).

## Review rejection 01 (seq=7)

- `src/ib/option_quote.rs` rejects `strike <= 0.0` only. `--strike NaN` and
  `--strike inf` therefore pass validation, reach `super::connect`, and return
  `connection` on a dead port instead of the required pre-connect `usage` envelope.
- Fix: validate finite-positive strikes before connecting, e.g.
  `!args.strike.is_finite() || args.strike <= 0.0` ⇒ `AppError::usage`.
- Keep `tests/option_quote_command.rs` frozen. Add source-local coverage only if useful.
