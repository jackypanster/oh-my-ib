---
id: "01"
title: "bounded take-first reads — timeout twin on the two PnL seams (ADR 0012)"
status: review
attempts: 0
verify:
  - "cargo build"
  - "cargo test --test read_timeouts"
spec-paths:
  - "tests/read_timeouts.rs"
impl-paths:
  - "src/error.rs"
  - "src/ib/mod.rs"
  - "src/ib/pnl.rs"
  - "src/ib/pnl_by_position.rs"
spec-rev: "3b011a62aa9004c63c67cc0dc6c5b7c48103325c"
---

# Card 01 — bounded take-first reads (read-timeouts)

Make `tests/read_timeouts.rs` green: bound BOTH take-first PnL reads with the crate's timeout
twin so no `omi` invocation can block forever on a wedged gateway PnL channel. Read `PRD.md` +
`arch.md` + **ADR 0012** first — `arch.md` §"Exact seam diffs" is the verbatim implementation;
do not deviate from it.

## Scope (impl-paths only — do NOT touch `tests/read_timeouts.rs`)

- `src/error.rs`:
  - `ErrorKind::Timeout` variant (doc: a bounded read produced no data in time — gateway-side
    wedge; the cure is a gateway restart).
  - `code()`: `Timeout => "timeout"`. `exit_code()`: `Timeout => 6`.
  - Constructor `pub fn timeout(message: impl Into<String>, context: impl Into<String>) -> Self`
    mirroring the siblings (kind + message + Some(context)).
- `src/ib/mod.rs`:
  - `pub const TAKE_FIRST_TIMEOUT: Duration = Duration::from_secs(10);` (file already has
    `use std::time::Duration`). Doc-comment: the shared bound for take-first reads on markerless
    streams (ADR 0012; fixed, not configurable — PRD D3).
- `src/ib/pnl.rs` (`pnl_with_client`): swap `subscription.next_data()` →
  `subscription.timeout_iter_data(super::TAKE_FIRST_TIMEOUT).next()`; `Some(Ok)`/`Some(Err)`
  arms byte-identical; `None` arm → `AppError::timeout` with the cure message (arch.md verbatim:
  `no PnL reading within {}s — gateway PnL channel may be wedged; restart the gateway`).
- `src/ib/pnl_by_position.rs` (`sweep_pnl_singles`): same swap on `sub`; `None` arm keeps conid
  attribution (arch.md verbatim: `pnl_single conid {conid}: no PnL reading within {}s — …`).
- Update the two files' module doc-comments where they describe the blocking read (they cite
  ADR 0007's blocking posture; now cite ADR 0012's bound).

## Hard constraints

- **Freeze gate**: `git diff 3b011a6 <your-tip> -- tests/read_timeouts.rs` must be EMPTY.
  Never create/modify/delete anything under `tests/` (repo-wide `cargo fmt` rewrites `tests/` —
  run fmt on `src/**` only, e.g. `cargo fmt -- src/error.rs src/ib/*.rs` or not at all).
- `src/ib/brief.rs`, `src/cli.rs`, `src/main.rs`, `src/output.rs`: NOT in impl-paths — brief
  inherits the bound through the two seams; there is NO new CLI/config surface.
- Healthy-path stdout must stay byte-identical on all four call paths (PRD criterion 6) — only
  the no-data failure arm changes.
- Public repo: no account ids, tokens, or balances anywhere.
- `verify` green = `cargo build` + `cargo test --test read_timeouts` (card-scoped). The full
  suite (`cargo test`) should also be green since this is the only card — run it before opening
  the PR; `cargo clippy --all-targets -- -D warnings` must stay clean.

## Freeze coverage

- **Frozen (`tests/read_timeouts.rs`)**: `AppError::timeout` ⇔ code `"timeout"` / exit 6 /
  Display `[timeout] msg (ctx)`; the full 7-row code⇔exit table (regression);
  `TAKE_FIRST_TIMEOUT == 10s`; `--help` surfaces gain no "timeout"; dead port stays
  `code="connection"`.
- **Review must READ (not freezable — no fake IB server, no-mock rule `agent_docs/tests.md`)**:
  both seam swaps use `timeout_iter_data(TAKE_FIRST_TIMEOUT).next()` exactly (not raw
  `next_timeout`, not a local const); cure messages match arch.md verbatim (duration + restart
  cure; sweep arm prefixes `pnl_single conid {conid}:`); `Some(Err)` arms and all surrounding
  code untouched; no other read site modified (the drain loops keep `iter_data()`).
- **Live (operator, PRD criterion 8, merge gate)**: `omi --live pnl` + `omi --live brief`
  healthy-path PASS in seconds — the bound must not slow the happy path.
