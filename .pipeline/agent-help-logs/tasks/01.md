---
status: in-progress
attempts: 0
spec-rev: 76ccd61df1f950fb15f9c34d94383a0e1e36e45e
verify: ["cargo build", "cargo test --test help_command"]
spec-paths: ["tests/help_command.rs"]
impl-paths: ["src/surface.rs", "src/cli.rs", "src/main.rs", "src/lib.rs"]
---

# Card 01 — `omi help`: one-shot agent-parseable command surface

Make `tests/help_command.rs` GREEN (4 red tests). Never edit spec-paths (freeze gate).
Read FIRST: `.pipeline/agent-help-logs/arch.md` (§Help output contract, §Resolutions),
`CONTEXT.md`, ADR 0036 (command_name is the shared anchor), `AGENTS.md`.

## Steps

1. `src/cli.rs`: add `disable_help_subcommand = true` to the `#[command(...)]` attrs on
   `Cli`; add BOTH new variants — `Help` and `Logs(LogsArgs)` (LogsArgs: `--tail`
   usize, default 50). Logs' variant must PARSE here so help's frozen inventory test
   (27 names incl. `logs`) can pass; its real behavior is card 02.
2. NEW `src/surface.rs`: `pub fn command_name(cmd: &Command) -> &'static str` — an
   EXHAUSTIVE match (no `_` arm; compile-time staleness anchor, also used by card 02's
   audit seam); the static help registry — per command: name / purpose / usage /
   example / gate, ALL non-empty, for ALL 27 commands (help + logs included); a
   `global` section for the global flags; `pub fn help_json() -> serde_json::Value`.
   Gate strings EXACT: `read-only` | `write` | `write-paper-only` (grid-tick, sma-tick).
   ADD an inline `#[cfg(test)]` unit test: clap subcommand names (via
   `<Cli as clap::CommandFactory>::command().get_subcommands()`) set-equal the registry
   names — the future-proof staleness guard (the frozen test only pins today's 27).
3. `src/lib.rs`: `pub mod surface;`.
4. `src/main.rs` `run()`: match `Command::Help` BEFORE `Config::load()` (must work with
   missing/broken config + no gateway) → `Ok(surface::help_json())`. `Command::Logs(_)`
   arm: temporary structured error `AppError::other("logs: implemented by card 02")` —
   never hit by this card's verify; card 02 replaces it.
5. `cargo build && cargo test --test help_command` green;
   `cargo clippy --all-targets -- -D warnings` clean; commit(s) on `feat/agent-help-logs`.

## Gotchas

- Keep main.rs's `DisplayHelp` / `DisplayVersion` arms intact — `omi --help` (flag) is a
  separately frozen contract (cli_contract.rs).
- `disable_help_subcommand` kills clap's `omi help <cmd>` passthrough — expected
  (arch-accepted consequence); do not try to restore it.
- Write purpose/usage/example from the REAL flags in cli.rs doc comments — an agent will
  act on these strings verbatim.

## Freeze coverage

Frozen: JSON shape (global + commands, 5 non-empty fields/entry), the exact 27-name
inventory, gate markers for 6 sampled commands, no-config/no-gateway operation.
Review must verify by READING: prose quality/accuracy of purpose/usage/example against
cli.rs; the inline clap-vs-registry set-equality unit test exists in surface.rs (the
future-proof guard is NOT frozen).
