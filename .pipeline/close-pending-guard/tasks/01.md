---
status: todo
attempts: 0
spec-rev: 61f17e8443b48083ec92a1a985ad06a35ebc7797
spec-paths:
  - tests/close_pending_guard.rs
impl-paths:
  - src/ib/trade.rs
  - src/ib/mod.rs
  - AGENTS.md
verify:
  - cargo build
  - cargo test --test close_pending_guard
---

# Card 01 — pending-close guard (blocking seam + option_close wiring)

One observable behaviour: `blocking_close_order_ids` returns exactly the working orders
that make a close unsafe, and `option_close` refuses when that set is non-empty.

## Do (all in src/ib/trade.rs unless noted)

1. Pure FROZEN seam (spec imports it):
   `pub fn blocking_close_order_ids(position: f64, conid: i32, open_orders: &[(i32, i32, String)]) -> Vec<i32>`
   — same conid ∧ opposite action (position>0 ⇒ "Sell" blocks; position<0 ⇒ "Buy" blocks;
   position==0 ⇒ empty); ids sorted ASCENDING. Action strings are exact "Buy"/"Sell".
2. Guard wiring in `option_close` — insert between the non-OPT usage check and
   `derive_close` (arch.md §Guard wiring VERBATIM):
   - `let rows = super::orders::open_orders_with_client(&client, None, ctx)?;` (SAME client
     — zero new connects).
   - Extract `(order_id, conid, action)` triples from the JSON rows; any row missing/
     malformed on those fields ⇒ `data` error naming the row index (NEVER skip — ADR 0023 §5).
   - Non-empty seam result ⇒ `not_found` error: message contains the conid, ALL blocking
     ids, and both remediation pointers (`omi cancel <id>`, `omi orders`), and states a
     second close would flip the position.
3. `src/ib/mod.rs`: add `blocking_close_order_ids` to the trade re-export line.
4. AGENTS.md: option-close phrase gains "; refuses while a working close order exists on
   the conid (anti double-fire)". **CLAUDE.md must NOT change.**

## Freeze coverage

- **Frozen** (tests/close_pending_guard.rs): the seam matrix (8 tests — block/no-block ×
  long/short, other-conid, multi-blocker ascending, zero-position totality, empty book).
- **Review must read**: guard placement (after non-OPT, before derive_close), single-connect
  (no second connect anywhere), fail-closed row parse, refusal message content, existing
  frozen suites byte-untouched (option_close_command.rs, positions_row.rs), CLAUDE.md
  untouched, containment polarity unchanged.
- **Deferred live** (PRD criterion 7): combined paper lifecycle with option-close criterion
  12 next US trading session.
