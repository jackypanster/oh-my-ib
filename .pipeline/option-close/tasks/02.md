---
status: done
attempts: 0
spec-rev: 286eb6ab0ea1728e8dfd32fe5be6a0443ae36016
spec-paths:
  - tests/option_close_command.rs
impl-paths:
  - src/ib/trade.rs
  - src/ib/mod.rs
  - src/cli.rs
  - src/main.rs
  - AGENTS.md
  - CLAUDE.md
verify:
  - cargo build
  - cargo test --test option_close_command
---

# Card 02 — `omi option-close` (close-by-conid write verb)

One observable behaviour: `omi option-close --conid N --limit PX [--qty N]` places the
derived opposite-side LMT/DAY order for a HELD option position, with the full offline
validation/gate matrix.

## Do (all logic in src/ib/trade.rs — the ONLY write module)

1. Pure FROZEN seams (exact signatures the spec imports):
   - `pub fn derive_close(position: f64, qty: Option<f64>) -> Result<(Action, f64), String>`
     — position sign ⇒ side (>0 SELL, <0 BUY, 0 ⇒ Err); qty None ⇒ |position|; Some ⇒ must
     be finite ∧ whole ∧ >= 1 ∧ <= |position| else Err (over-close never flips).
   - `pub fn shape_option_close_ack(order_id: i32, status: &str, conid: i32, symbol: &str,
     expiry: &str, strike: f64, right: &str, action: &str, quantity: f64, limit_price: f64)
     -> Value` — exact 10-key object (see frozen test for key set/values).
2. Gateway fn `option_close(cfg, &OptionCloseArgs)` per
   `.pipeline/option-close/arch.md` §Data flow (SINGLE-connect, steps 1-9 in order):
   usage validation (conid >= 1; limit finite > 0; qty per seam rules — validate BEFORE
   gate) → `require_live_write_gate` → `super::connect` ONCE → `resolve_account` →
   `account_updates` drain to End, LAST row with `contract_id == conid` wins →
   not-held/position==0 ⇒ `not_found`; non-OPT ⇒ `usage` naming the sec_type →
   `derive_close` → `parse_expiry(raw)` else `data` naming the raw value →
   `build_option_order` VERBATIM (trading_class from row iff non-empty, "SMART",
   row currency iff non-empty else "USD") → `contract_details` FIRST-row conid must ==
   `--conid` else `data` ("refusing to place") → `place_with_client` with the 10-key ack.
3. `src/cli.rs`: `OptionClose(OptionCloseArgs)`; args ALL `#[arg(long)]`:
   `conid: i32`, `limit: f64` (required), `qty: Option<f64>`; doc-comment example line.
4. `src/main.rs`: dispatch arm. `src/ib/mod.rs`: re-export
   `pub use trade::{derive_close, option_close, shape_option_close_ack, ...}` (keep the
   existing trade re-exports intact).
5. Docs amendment (rides this PR): AGENTS.md Phase-2 line + CLAUDE.md options sentence gain
   `option-close` (close-by-conid, side derived from held position). **`wc -c CLAUDE.md`
   MUST stay < 900** (computed headroom: 861+15=876 with `/`option-close`` inserted).

## Freeze coverage

- **Frozen** (tests/option_close_command.rs): `derive_close` matrix (anti-double gate),
  10-key ack, gate parity (config before connection; effective-port), usage matrix
  (conid/limit/qty, pre-connect ordering), `--help` surface.
- **Review must read**: the gateway fn — single-connect (NEVER a second connect,
  option-combo lesson), anti-open gate (`not_found` before any placement), wrong-contract
  gate (`contract_details` assert BEFORE `place_with_client`), bounded first-ack + no-retry
  (ADR 0017 verbatim), containment polarity grep (write calls only in trade.rs), docs
  amendment + CLAUDE.md byte budget.
- **DELIBERATE spec omission**: no env+live+dead-port gate-pass test (would place a REAL
  live order if a gateway is up — option-orders precedent; shared gate covered by stk twins).
