---
status: review
attempts: 0
spec-rev: 286eb6ab0ea1728e8dfd32fe5be6a0443ae36016
spec-paths:
  - tests/positions_row.rs
impl-paths:
  - src/ib/positions.rs
  - src/ib/mod.rs
verify:
  - cargo build
  - cargo test --test positions_row
---

# Card 01 — positions identity enrichment (14-key row)

One observable behaviour: `position_row` emits the exact 14-key row
(legacy 9 byte-identical + `sec_type`/`expiry`/`strike`/`right`/`multiplier`).

## Do

1. `src/ib/positions.rs`: promote `position_row` `pub(crate)` → `pub` (frozen-seam
   precedent: `assemble_brief`). Append the 5 identity keys per
   `.pipeline/option-close/arch.md` §Card 01 table:
   - `sec_type`: `p.contract.security_type.to_string()` (Display ⇒ wire code "STK"/"OPT").
   - `expiry`/`strike`/`right`/`multiplier`: populated **iff**
     `matches!(p.contract.security_type, SecurityType::Option)`, else ALL `Value::Null`.
     `expiry` = raw `last_trade_date_or_contract_month` passthrough; `right` maps
     `Some(Call)⇒"C"`, `Some(Put)⇒"P"`, anything else ⇒ null (non_exhaustive-safe `_` arm);
     `multiplier` = string passthrough, empty string ⇒ null.
2. `src/ib/mod.rs`: `pub use positions::{position_row, positions};`.
3. No other file. `brief` parity is automatic (same fn) — do NOT touch brief.rs.

## Freeze coverage

- **Frozen** (tests/positions_row.rs): 14-key set, null semantics (STK ⇒ 4 nulls), legacy-9
  byte-identity, long/short sign passthrough, empty-multiplier ⇒ null, OPT identity values.
- **Review must read**: gateway drains unchanged (`positions()`/`brief()` bodies untouched
  beyond the row fn); no write-module imports in read modules (polarity grep); the
  non_exhaustive `right` fallback arm (unconstructible from tests/).
