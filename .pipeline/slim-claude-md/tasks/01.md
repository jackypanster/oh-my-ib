---
id: "01"
title: "Slim CLAUDE.md to a pointer at AGENTS.md"
status: review
attempts: 0
verify:
  - "cargo build"
  - "cargo test --test claude_md"
spec-paths:
  - "tests/claude_md.rs"
impl-paths:
  - "CLAUDE.md"
spec-rev: "b61e9a583049ce4b67db971240d8728837b98890"
---

# Card 01 — slim CLAUDE.md to a pointer

Make `tests/claude_md.rs` green by replacing CLAUDE.md's duplicated body with a short pointer.

## Scope
- Rewrite `CLAUDE.md` (~4316 bytes → ~400-700) as a pointer:
  - "Read **AGENTS.md** first — it is the canonical agent-conventions doc."
  - One-line backstops: public repo (no secrets); read-only; live (`:4001`) needs `--live`, writes need
    `OMI_ALLOW_LIVE=1`; pipeline-driven — read `CONTRACT.md` (jackypanster/pipeline) then `.pipeline/<feature>/`.
- Must contain `AGENTS.md`; total length 100 < len < 900.

## Freeze coverage
Frozen (`tests/claude_md.rs`): CLAUDE.md references AGENTS.md and stays short. Fully offline — docs
change, no gateway/live acceptance. Do NOT touch AGENTS.md.
