---
id: "01"
title: "variadic omi quote — batch snapshots on one session (ADR 0013)"
status: review
attempts: 0
verify:
  - "cargo build"
  - "cargo test --test multi_quote"
spec-paths:
  - "tests/multi_quote.rs"
impl-paths:
  - "src/cli.rs"
  - "src/ib/quote.rs"
  - "src/ib/mod.rs"
spec-rev: "828348aa1124bd15d0ed39d26fe29097fea19aea"
---

# Card 01 — variadic quote (multi-quote)

Make `tests/multi_quote.rs` green: `omi quote SYM1 [SYM2 …]` — N snapshot quotes on ONE
connection, ONE `switch_market_data_type`, input order, N-polymorphic output. Read `PRD.md` +
`arch.md` + **ADR 0013** first — `arch.md` §"Component design" is the verbatim implementation;
do not deviate.

## Scope (impl-paths only — do NOT touch `tests/`)

- `src/cli.rs` (`QuoteArgs`):
  - `pub symbol: String` → `pub symbols: Vec<String>` with `#[arg(required = true, value_name = "SYMBOL")]`.
  - Doc-comment/help MUST contain the literal `symbol(s)` (frozen: help test), e.g.
    `/// Ticker symbol(s), e.g. AAPL MSFT`.
  - Other fields (`sec_type`, `exchange`, `currency`) untouched.
- `src/ib/quote.rs`:
  - Extract `pub(crate) fn quote_one(client: &Client, symbol: &str, exchange: &str, currency: &str, delayed: bool) -> Result<Value, AppError>`
    — EXACTLY today's contract build + snapshot subscribe + SnapshotEnd drain +
    `quote_price_tick` filter + `json!({"symbol", "delayed", "ticks"})` assembly, with error
    contexts `format!("quote/{symbol}")` on both the subscribe and stream arms.
  - Add the pure seam (frozen shape):
    `pub fn shape_quotes(mut rows: Vec<Value>) -> Value` — 1 ⇒ `rows.pop()` bare object
    untouched, else ⇒ `Value::Array(rows)` (covers 0 ⇒ `[]`).
  - `pub fn quote(cfg, args)`: STK guard (unchanged, before connect) → `super::connect` →
    md-type match + `switch_market_data_type` ONCE (unchanged block, `delayed` computed once) →
    `for symbol in &args.symbols` in order: `rows.push(quote_one(…)?)` (fail-fast `?`) →
    `Ok(shape_quotes(rows))`.
- `src/ib/mod.rs`: re-export `shape_quotes` alongside the existing
  `pub use quote::{quote, quote_price_tick};`.

## Hard constraints

- **Freeze gate**: `git diff 828348a <your-tip> -- tests/multi_quote.rs` must be EMPTY. Never
  touch `tests/` (repo-wide `cargo fmt` rewrites it — fmt `src/**` only or skip).
- N=1 output byte-identical to pre-feature (PRD criterion 1): `quote_one`'s `json!` literal is
  today's literal; `shape_quotes` returns it bare. Do NOT add keys (no `as_of`, no count).
- `src/main.rs` / `src/output.rs` / `src/error.rs`: NOT in impl-paths — dispatch, renderer, and
  envelope are untouched by design.
- Existing frozen quote surfaces must stay green: `tests/quote_ticks.rs`,
  `tests/data_commands.rs` (quote help still mentions md-type; `quote AAPL` dead-port).
- Snapshot drains are SnapshotEnd-BOUNDED — do NOT wrap in `TAKE_FIRST_TIMEOUT` /
  `timeout_iter_data` (ADR 0012 is for markerless streams only; ADR 0013 records this).
- Public repo: no account ids, tokens, or balances anywhere.
- `verify` = `cargo build` + `cargo test --test multi_quote` (card-scoped). Before the PR: full
  `cargo test` green (single card) + `cargo clippy --all-targets -- -D warnings` clean.

## Freeze coverage

- **Frozen (`tests/multi_quote.rs`)**: `shape_quotes` (1 ⇒ bare object EXACT-equality; 2/3 ⇒
  bare array in input order, rows untouched; duplicates preserved; 0 ⇒ `[]`); CLI (zero
  symbols ⇒ `code="usage"`; two symbols dead-port ⇒ `code="connection"`; help contains
  `symbol(s)`).
- **Review must READ (not freezable offline)**: one connect + ONE switch per invocation; the
  loop order + fail-fast `?`; `quote/<symbol>` contexts; `quote_one` literal identical to
  today's assembly; no timeout wrapping on the drain.
- **Live (operator, PRD criterion 9, merge gate)**: same-session `omi --live quote AAPL` +
  `omi --live quote AAPL MSFT NVDA`; single object matches the batch row shape.
