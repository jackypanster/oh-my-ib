---
id: "01"
title: "omi completed-orders — terminal order states via reqCompletedOrders (ADR 0015)"
status: todo
attempts: 0
verify:
  - "cargo build"
  - "cargo test --test completed_orders_command"
spec-paths:
  - "tests/completed_orders_command.rs"
impl-paths:
  - "src/cli.rs"
  - "src/ib/completed_orders.rs"
  - "src/ib/mod.rs"
  - "src/main.rs"
spec-rev: "aff35991c759a0bbfd44dd03ea1d67fac0241dbf"
---

# Card 01 — omi completed-orders

Make `tests/completed_orders_command.rs` green: read-only `omi completed-orders` — today's
terminal-state orders via ONE `completed_orders(false)` drain-to-End. Read `PRD.md` +
`arch.md` + **ADR 0015** first — `arch.md` §Component design is the verbatim implementation.

## Scope (impl-paths only — do NOT touch `tests/`)

- `src/cli.rs`: `CompletedOrders` variant, doc "Today's completed orders (filled/cancelled)
  with status". No args beyond globals.
- `src/ib/completed_orders.rs` (NEW): `CompletedOrderRow` (14 pub fields exactly as the
  frozen test constructs), `pub fn shape_completed_orders(rows) -> Value` (gateway order →
  array of exact 14-key objects; `limit_price`/`aux_price` raw `Option<f64>` → null on None;
  empty ⇒ `json!([])`), `pub fn completed_orders(cfg) -> Result<Value, AppError>` (connect →
  `client.completed_orders(false)` → drain `iter_data()`, `Orders::OrderData` arm only,
  filter rows when `cfg.account` set (`orders` semantics) → wrap
  `{"completed_orders": shape_completed_orders(rows)}`). Contexts: `completed-orders`.
  Field mapping per arch.md §ibapi facts (Debug-render action/tif/status; strings verbatim).
- `src/ib/mod.rs`: `mod completed_orders;` +
  `pub use completed_orders::{completed_orders, shape_completed_orders, CompletedOrderRow};`
- `src/main.rs`: `Command::CompletedOrders => ib::completed_orders(&config),`

## Hard constraints

- **Freeze gate**: `git diff aff3599 <your-tip> -- tests/completed_orders_command.rs` EMPTY.
  Never touch `tests/`; never repo-wide `cargo fmt` (fmt `src/**` only or skip).
- **READ-ONLY red line**: no place/modify/cancel code anywhere in the diff (public repo,
  trading is a later gated phase).
- Drain class: End-marked, self-terminating — NO `TAKE_FIRST_TIMEOUT` (ADR 0012 boundary).
- `orders.rs`/`brief.rs`/`output.rs`/`error.rs`: untouched (brief's top level is frozen —
  no new section).
- Public repo: no account ids/tokens/balances.
- `verify` card-scoped; before PR: full `cargo test` green + clippy `-D warnings` clean.

## Freeze coverage

- **Frozen (`tests/completed_orders_command.rs`)**: `shape_completed_orders` (exact 14-key
  set; verbatim values; None prices → present null keys; "" completion strings pass through;
  gateway order; zero ⇒ `[]`); CLI (help lists `completed-orders`; subcommand help ok; dead
  port ⇒ `code="connection"`).
- **Review must READ**: the drain fn — `completed_orders(false)` hardcoded, OrderData-only
  arm, filter-when-set parity with `orders.rs`, wrapper key, contexts; READ-ONLY grep
  (place/modify/cancel absent).
- **Live (operator, PRD criterion 8, merge gate)**: `omi --live completed-orders` shape/exit
  PASS; `[]` on a no-trade day is a PASS (row content rides the first trading day).
