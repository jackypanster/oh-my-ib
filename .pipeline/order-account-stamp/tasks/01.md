---
status: todo
attempts: 0
spec-rev: 24a34dd0c10135eb77981b882e55a1eb4ff1d74c
spec-paths:
  - tests/order_account_stamp.rs
impl-paths:
  - src/ib/trade.rs
  - src/ib/mod.rs
  - AGENTS.md
verify:
  - cargo build
  - cargo test --test order_account_stamp
---

# Card 01 — order-account-stamp (resolved-account choke-point stamping)

One observable behaviour: `stamp_order_account` sets exactly `Order.account`, and every
placement path stamps the resolved account before `place_order`.

## Do (all in src/ib/trade.rs unless noted)

1. Pure FROZEN seam (spec imports it):
   `pub fn stamp_order_account(order: &mut Order, account: &str)` — sets `order.account`
   (overwriting any prior value), touches nothing else. One line.
2. `place_with_client`: add a REQUIRED `account: &AccountId` parameter (arch.md §Choke
   point VERBATIM). Body: `let mut order = order.clone(); stamp_order_account(&mut order,
   &account.0);` then the EXISTING allocate → `place_order(&order)` → bounded first-ack
   UNCHANGED. `Order` derives `Clone` (verified).
3. Call sites pass the resolved account:
   - `place_core`: after its `connect`, `let account = super::resolve_account(&client,
     cfg)?;` → pass to `place_with_client`.
   - `option_combo`: resolve after connect (one bounded call) → pass through.
   - `option_close`: pass its ALREADY-resolved `account` (no second `managed_accounts`).
4. `src/ib/mod.rs`: add `stamp_order_account` to the trade re-export line.
5. AGENTS.md: Phase-2 orders sentence gains "orders are stamped with the resolved account
   (`--account` honored on writes)". **CLAUDE.md must NOT change.**

## Freeze coverage

- **Frozen** (tests/order_account_stamp.rs): the seam matrix (stamps empty; overwrites
  pre-set; whole-struct byte-identity for LMT and MKT; empty-string degenerate).
- **Review must read**: choke-point wiring (required param, clone-then-stamp, exactly one
  `.place_order(` on the stamped path), place_core/option_combo resolve-after-connect,
  option_close reuse (no duplicate managed_accounts), all six existing frozen suites
  byte-untouched (stk/option/combo/option_close/positions_row/close_pending_guard),
  CLAUDE.md untouched, AGENTS.md phrase.
- **Deferred/live** (PRD criterion 7, runnable TODAY): far-off option-buy ⇒ `omi orders`
  row `account == DUQ653733` ⇒ cancel. Gateway rejection of explicit account ⇒
  observation + operator decision (ADR 0024 §5 fallback).
