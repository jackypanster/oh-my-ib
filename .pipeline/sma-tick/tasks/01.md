---
id: "01"
title: "sma-tick — active 200-day month-end timing executor (QQQ, lot 10, paper v1) (ADR 0035)"
status: review
attempts: 1
verify:
  - "cargo build"
  - "cargo test --test sma_tick"
spec-paths:
  - "tests/sma_tick.rs"
impl-paths:
  - "src/ib/sma_tick.rs"
  - "src/ib/signal.rs"
  - "src/ib/mod.rs"
  - "src/cli.rs"
  - "src/main.rs"
spec-rev: "22b1a9ebe6a541e31ac2b1de0f9132a476f95b3f"
---

# Card 01 — sma-tick (active 200SMA timing executor)

Make `tests/sma_tick.rs` green per **ADR 0035**. Read `PRD.md` + `arch.md` (exact types + the gateway
steps) + `docs/adr/0035-sma-tick.md` + `CONTEXT.md` FIRST. WRITE-orchestration card, **paper-only** —
compose the trade.rs choke points (ADR 0017); NO raw `place_order` in `sma_tick.rs`. The frozen test
freezes the PURE `plan_sma_tick`; the gateway `sma_tick_cmd` + `signal_for` extraction + wiring are
review-by-reading. This is the grid-tick shape (mirror `src/ib/grid.rs`).

## Scope (impl-paths only — do NOT touch `tests/`)

### 1. `src/ib/sma_tick.rs` (NEW) — frozen pure seam + gateway

```rust
#[derive(Debug, Clone, Copy, PartialEq)]
pub enum TickAction { Buy { qty: f64 }, Sell { qty: f64 }, Noop }
```

**Pure `pub fn plan_sma_tick(state: SignalState, current_qty: f64, lot: f64) -> TickAction`**:
```
target = match state { Hold => lot, Exit => 0.0, Insufficient => return Noop }
delta  = target - current_qty
if delta >  1e-9 → Buy { qty: delta }
else if delta < -1e-9 → Sell { qty: -delta }
else → Noop
```
`SignalState` is `crate::ib::SignalState` (from signal.rs). f64 throughout; no `==` on floats (uses `>`/`<`).

**Gateway `pub fn sma_tick_cmd(cfg: &Config, args: &SmaTickArgs) -> Result<Value, AppError>`** (review-by-reading):
1. `if cfg.port == LIVE_PORT { return Err(AppError::config("sma-tick is paper-only in v1 — use paper :4002", "sma-tick")); }` (offline, before connect).
2. `let symbol = args.symbol.clone().unwrap_or_else(|| "QQQ".into());`
3. `let client = super::connect(cfg)?; let account = super::resolve_account(&client, cfg)?;`
4. `let sig = super::signal::signal_for(&client, &symbol, args.sma)?;` (see §2 — the extracted helper).
5. current qty: `super::positions(cfg)?` → find the row with `symbol` == symbol, read its `qty` (0.0 if absent).
   (positions() connects again — acceptable; or read via the same client if you thread it. Reuse positions() is fine.)
6. `let action = plan_sma_tick(sig.state, current_qty, args.lot);`
7. `if args.dry_run` → return `json!({symbol, signal: sig.state.as_str(), as_of, current_qty, target_qty,
   action: shape(action), dry_run: true})` — NO order.
8. else match action:
   - `Buy { qty }`  → `price = round2(sig.latest_close * 1.02)`; side = Action::Buy.
   - `Sell { qty }` → `price = round2(sig.latest_close * 0.98)`; side = Action::Sell.
   - `Noop` → return the envelope with no order.
   Then `let (contract, order) = super::build_stk_order(&symbol, side, qty, Some(price));`
   `super::place_with_client(&client, "sma-tick", &contract, &order, &account, |id,st| json!({...ack...}))`.
9. Return `json!({symbol, signal, as_of, current_qty, target_qty, action, order?})`.
`round2(x) = (x*100.0).round()/100.0`.

### 2. `src/ib/signal.rs` — extract `signal_for` (keep sma-signal byte-identical)

Extract `pub(crate) fn signal_for(client: &Client, symbol: &str, n: usize) -> Result<SmaSignal, AppError>`
= the fetch-2Y-Day-bars + map-to-Bar + `sma_signal(&bars, n)` currently inline in `sma_signal_cmd`.
`sma_signal_cmd` now CALLS `signal_for` (its behavior + JSON stay byte-identical → its frozen tests +
`tests/sma_signal.rs` stay GREEN). `sma_tick_cmd` reuses it. (SmaSignal already carries `latest_close`.)

### 3. `src/ib/mod.rs` — `mod sma_tick;` + `pub use sma_tick::{plan_sma_tick, sma_tick_cmd, TickAction};`

### 4. `src/cli.rs` — `SmaTick(SmaTickArgs)`

`pub struct SmaTickArgs { /// symbol (default QQQ) pub symbol: Option<String>,
#[arg(long, default_value_t = 10.0)] pub lot: f64, #[arg(long, default_value_t = 200)] pub sma: usize,
#[arg(long)] pub dry_run: bool }`. Doc: "Reconcile the position to the 200-day month-end signal (paper-only)."

### 5. `src/main.rs` — dispatch `SmaTick(a) => ib::sma_tick_cmd(&cfg, a)`.

## Out of scope — MUST stay byte-identical / absent

- **NO write path change / NO raw place_order/cancel_order in sma_tick.rs** — compose `build_stk_order` +
  `place_with_client` only (ADR 0017; review greps). **Paper-only**: refuse `cfg.port == LIVE_PORT`.
- `trade.rs`, grid-tick (`grid.rs`), the live gate / notional cap / combo breaker — untouched.
- `signal.rs`: only the `signal_for` extraction (sma_signal / sma_signal_cmd behavior byte-identical;
  `tests/sma_signal.rs` + all prior suites stay GREEN).
- No MKT (marketable LMT only), no live, no multi-symbol/toml, no laddering, no scheduling/Telegram.

## Freeze coverage

- **FROZEN** (`tests/sma_tick.rs`, spec-rev `22b1a9e`): `plan_sma_tick` (Hold+flat⇒Buy lot; at-target⇒Noop;
  partial⇒Buy diff; over⇒Sell down; Exit+held⇒Sell all; Exit+flat⇒Noop; Insufficient⇒Noop). RED =
  unresolved `oh_my_ib::ib::plan_sma_tick` import.
- **REVIEW-BY-READING**: gateway `sma_tick_cmd` (paper-only guard, `signal_for` reuse, position read,
  marketable-LMT price, `build_stk_order`+`place_with_client` execution, `--dry-run`, JSON); the
  `signal_for` extraction (sma-signal byte-identical); containment grep (no raw place_order); CLI wiring.
- **OPERATOR ACCEPTANCE** (paper `:4002`): `omi sma-tick QQQ --lot 10 --dry-run` shows signal + current/
  target/action; a real run places the reconcile LMT; `omi --live sma-tick QQQ` refused paper-only.

## Done when

`cargo test --test sma_tick` GREEN, `cargo build` OK, `cargo clippy --all-targets -- -D warnings` clean,
all prior suites (incl. `sma_signal`, `grid_tick`, the 4 write suites) GREEN and byte-identical,
`spec-paths` untouched. Then open the PR from `feat/sma-tick`. Full-suite + operator paper at review.

## Review rejection 01 -- 2026-07-07 -- codex

REJECT: two write-path fail-closed issues in `src/ib/sma_tick.rs`.
First, `current_position_qty` swallows any `positions(cfg)` error and returns `0.0`, so an unknown
position state can be treated as flat and used for planning/placement. The card specified
`super::positions(cfg)?`; only an absent symbol in a successful positions payload should mean 0.0.
Second, `--lot` is not validated as finite and positive before planning; negative lot can invert HOLD into
a Sell and non-finite lot can reach `build_stk_order`. Fix both at the command boundary / position-read
boundary, keep `tests/sma_tick.rs` untouched, and preserve ADR 0017 containment.
