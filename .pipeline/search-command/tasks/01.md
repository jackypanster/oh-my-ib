---
id: "01"
title: "omi search — fuzzy symbol lookup via reqMatchingSymbols (ADR 0014)"
status: done
attempts: 0
verify:
  - "cargo build"
  - "cargo test --test search_command"
spec-paths:
  - "tests/search_command.rs"
impl-paths:
  - "src/cli.rs"
  - "src/ib/search.rs"
  - "src/ib/mod.rs"
  - "src/main.rs"
spec-rev: "db074c66d37f0dce8544cd9a84e6dadbf33f976d"
---

# Card 01 — omi search (search-command)

Make `tests/search_command.rs` green: read-only `omi search <pattern>` — fuzzy symbol/company
lookup via ONE `matching_symbols` call, full pass-through rows. Read `PRD.md` + `arch.md` +
**ADR 0014** first — `arch.md` §Component design is the verbatim implementation.

## Scope (impl-paths only — do NOT touch `tests/`)

- `src/cli.rs`: `Search(SearchArgs)` variant (doc "Fuzzy symbol/company search") +
  `pub struct SearchArgs { /// Search text, e.g. apple or "hong kong"
  pub pattern: String }` (single required positional).
- `src/ib/search.rs` (NEW): `SearchRow` (7 pub fields, exactly as the frozen test constructs),
  `pub fn shape_search(rows: Vec<SearchRow>) -> Value` (gateway order → array of exact 7-key
  objects; empty ⇒ `json!([])`), `pub fn search(cfg, args) -> Result<Value, AppError>`
  (connect → `client.matching_symbols(&args.pattern)` → map fields via `.to_string()` on the
  newtype wrappers → `shape_search`). Error: `AppError::data(format!("matching_symbols
  failed: {e}"), "search")`.
- `src/ib/mod.rs`: `mod search;` + `pub use search::{search, shape_search, SearchRow};`
- `src/main.rs`: `Command::Search(args) => ib::search(&config, args),`

## Hard constraints

- **Freeze gate**: `git diff db074c6 <your-tip> -- tests/search_command.rs` must be EMPTY.
  Never touch `tests/`; never repo-wide `cargo fmt` (fmt `src/**` only or skip).
- **NO STK guard, no account resolution, no md-type switch** (ADR 0014 — metadata read; do
  NOT copy `quote`'s preamble). No `TAKE_FIRST_TIMEOUT` (plain bounded call, not markerless).
- Strings pass through as-is ("" stays ""); no `pnl_number` on any field (nothing is money).
- No re-sorting/re-ranking — gateway order verbatim.
- `output.rs`/`error.rs`: untouched. Public repo: no secrets.
- `verify` = card-scoped; before the PR: full `cargo test` green +
  `cargo clippy --all-targets -- -D warnings` clean.

## Freeze coverage

- **Frozen (`tests/search_command.rs`)**: `shape_search` (exact 7-key row, order verbatim,
  "" pass-through, `[]` derivative key present, non-STK rows kept, zero ⇒ `[]`); CLI (help
  lists search; `search --help` ok; missing pattern ⇒ `usage`; dead port ⇒ `connection`).
- **Review must READ**: the gateway fn — one `matching_symbols` call, field mapping exact
  (conid/symbol/sec_type/primary_exchange/currency/description/derivative_sec_types), `data`
  context `search`, no guard/no timeout/no extra calls.
- **Live (operator, PRD criterion 8, merge gate)**: `omi --live search apple` non-empty with
  an AAPL row; one non-US pattern spot-check (informational).
