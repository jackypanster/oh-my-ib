---
id: "01"
title: "option-orders — omi option-buy/option-sell: single-leg LMT/DAY, gated write path (ADR 0020)"
status: in-progress
attempts: 0
verify:
  - "cargo build"
  - "cargo test --test option_orders_command"
spec-paths:
  - "tests/option_orders_command.rs"
impl-paths:
  - "src/cli.rs"
  - "src/ib/trade.rs"
  - "src/ib/option_quote.rs"
  - "src/ib/mod.rs"
  - "src/main.rs"
  - "AGENTS.md"
  - "CLAUDE.md"
spec-rev: "63f3232ca38e7ebf6a7df9e07705f8f52c049101"
---

# Card 01 — option-orders (Phase 2 step 3: single-leg option WRITE path)

Make `tests/option_orders_command.rs` green: `omi option-buy` / `omi option-sell` per
**ADR 0020**. Read `PRD.md` + `arch.md` + ADR 0020 + `CONTEXT.md` first — arch.md
§Component design + §CLI + §Docs amendment are the verbatim implementation. HIGH-STAKES
write-path card: safety machinery is ADR 0017/0018 VERBATIM REUSE, zero new mechanisms.

## Scope (impl-paths only — do NOT touch `tests/`)

- `src/cli.rs`: `OptionBuy(OptionOrderArgs)`, `OptionSell(OptionOrderArgs)`;
  `OptionOrderArgs` VERBATIM from arch.md §CLI (`--limit` is a required plain f64 —
  LMT-only).
- `src/ib/trade.rs` (the ONLY write module):
  - `pub fn build_option_order(symbol: &str, expiry: (u16, u8, u8), strike: f64,
    right: OptionRight, trading_class: Option<&str>, exchange: &str, currency: &str,
    side: Action, quantity: f64, limit: f64) -> (Contract, Order)` — options-read builder
    chain (`Contract::call/put .strike .expires_on .on_exchange .in_currency
    [.trading_class] .build()`); Order = LMT/Some(limit)/Day always.
  - `pub fn shape_option_order_ack(order_id: i32, status: &str, symbol: &str, expiry: &str,
    strike: f64, right: &str, action: &str, quantity: f64, limit_price: f64) -> Value` —
    exact 9-key.
  - **place-core extraction (ADR 0020 D3)**: refactor `place()`'s gate→connect→allocate→
    place_order→bounded-first-ack into `fn place_core(cfg, ctx, &Contract, &Order,
    ack: impl Fn(i32, &str) -> Value) -> Result<Value, AppError>`; stk `place()` and new
    `place_option()` both delegate. STK BEHAVIOR MUST STAY BYTE-IDENTICAL — the frozen stk
    suite (tests/stk_orders_command.rs) is the regression net and must stay green.
  - `pub fn option_buy/option_sell(cfg, &OptionOrderArgs)`: validation FIRST (usage <
    config < connection, frozen ordering): right via option_quote's normalize_right,
    expiry via parse_expiry, strike finite>0, qty finite ∧ >=1 ∧ fract()==0, limit
    finite>0 → `require_live_write_gate` → connect → `next_order_id()` →
    `build_option_order` → `place_core` with the 9-key ack closure (right normalized,
    expiry echoed as the original string). Contexts "option-buy"/"option-sell".
- `src/ib/option_quote.rs`: VISIBILITY-ONLY — `normalize_right` + `parse_expiry` →
  `pub(crate)`. Nothing else.
- `src/ib/mod.rs`: re-export `option_buy, option_sell, build_option_order,
  shape_option_order_ack`.
- `src/main.rs`: two dispatch arms.
- `AGENTS.md` + `CLAUDE.md`: arch.md §Docs amendment VERBATIM, nothing else.

## Hard constraints

- **Freeze gate**: `git diff 63f3232 <tip> -- tests/option_orders_command.rs` EMPTY.
  Never touch tests/ (incl. the stk spec); never repo-wide `cargo fmt`.
- Write calls stay ONLY in trade.rs (polarity grep); no retry logic; no MKT arm; reuse
  `TAKE_FIRST_TIMEOUT`; no new consts/error codes/deps.
- Validation BEFORE gate BEFORE connect (frozen tests assert the ordering).
- Public repo: no account ids/tokens/balances.
- `verify` card-scoped; before PR: FULL `cargo test` green (whole suite — single card) +
  clippy `-D warnings` clean.

## Freeze coverage

- **Frozen (tests/option_orders_command.rs, 22 tests, offline)**: build_option_order exact
  fields (call/buy, put/sell+trading_class, zero-padded expiry); 9-key ack; gate matrix
  (both verbs live-no-env ⇒ config, --port 4001 ⇒ config, paper dead-port ⇒ connection);
  validation matrix (qty 0/1.5/inf, limit missing/0/NaN, strike inf, right X, dashed
  expiry ⇒ usage; usage-precedes-gate probe); help surface.
  **DELIBERATE OMISSION (documented in the spec header): no env+live+dead gate-pass test —
  the stk twin covers the shared gate; an option twin would fire a real live order if the
  live gateway were up during a test run.**
- **Review must READ**: place_core extraction diff (stk byte-identity), containment grep,
  option_quote.rs visibility-only diff (exactly two pub(crate) additions), docs amendment
  verbatim, no-MKT absence, no-retry.
- **Live (operator, paper :4002, PRD criterion 10, merge gate)**: far-below-market LMT
  option-buy → orders shows working → cancel → completed-orders Cancelled → positions
  unchanged. Paper options-permission rejection = journaled observation, NOT impl failure.
