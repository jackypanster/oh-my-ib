---
id: "01"
title: "Track AGENTS.md with the agent-first authoring convention"
status: done
attempts: 0
verify:
  - "cargo build"
  - "cargo test --test agents_md"
spec-paths:
  - "tests/agents_md.rs"
impl-paths:
  - "AGENTS.md"
spec-rev: "05d685f155deef277f89678001ae72f1e798b01b"
---

# Card 01 — track AGENTS.md (agent-first conventions)

Make `tests/agents_md.rs` green by writing + tracking the canonical `AGENTS.md`.

## Scope
- Author `AGENTS.md` at the repo root and `git add` it (it is currently untracked). Sections:
  - One-paragraph project map: `omi` = read-only IBKR/Tiger CLI, agent-driven; `--live` (4001) /
    paper (4002); no API keys; HKT timezone auto-registered.
  - `## Authoring (agent-first)` — the operator's rule: docs AND code written for agents, not humans.
    Dense/structured docs; explicit types + stable JSON/exit-code interfaces; JSON output first,
    human/table secondary; when unsure "human or agent?" assume agent.
  - `## Hard safety rules` — read-only Phase 1; public repo (no account ids/secrets); live needs
    `--live`, write needs `OMI_ALLOW_LIVE=1`.
  - `## How this repo is built` — pipeline-driven; read `CONTRACT.md` in jackypanster/pipeline first,
    then `.pipeline/<feature>/` (PRD/arch/journal tail). Point at CONTRACT.md, do not copy it.
- Must contain the frozen markers: `agent-first`, `Authoring (agent-first)`, `CONTRACT.md`, `OMI_ALLOW_LIVE`; len > 800.

## Freeze coverage
Frozen (`tests/agents_md.rs`): AGENTS.md exists + contains the markers. Fully offline — no gateway,
no live acceptance (docs/convention change). Do NOT modify CLAUDE.md (out of scope).
