---
id: "01"
title: "stk-orders — buy/sell/cancel write path, paper-first, double-gated (ADR 0017)"
status: done
attempts: 1
verify:
  - "cargo build"
  - "cargo test --test stk_orders_command"
spec-paths:
  - "tests/stk_orders_command.rs"
impl-paths:
  - "src/cli.rs"
  - "src/ib/trade.rs"
  - "src/ib/mod.rs"
  - "src/main.rs"
  - "AGENTS.md"
  - "CLAUDE.md"
spec-rev: "3692c71bc11e873b2be8f3c9448a2a8d4f4d9e8f"
---

# Card 01 — stk-orders (PHASE 2 opener: the write path)

Make `tests/stk_orders_command.rs` green: `omi buy` / `omi sell` / `omi cancel` per
**ADR 0017**'s safety architecture. Read `PRD.md` + `arch.md` + ADR 0017 first — arch.md
§Component design is the verbatim implementation. HIGHEST-STAKES card in this repo.

## Scope (impl-paths only — do NOT touch `tests/`)

- `src/cli.rs`: `Buy(OrderArgs)`, `Sell(OrderArgs)`, `Cancel(CancelArgs)`;
  `OrderArgs { symbol: String, quantity: f64, #[arg(long)] limit: Option<f64> }`;
  `CancelArgs { order_id: i32 }`. Docs: buy "Place a BUY order (paper by default; live needs
  --live + OMI_ALLOW_LIVE=1)", sell/cancel analogous.
- `src/ib/trade.rs` (NEW — the ONLY file with write calls):
  - `pub fn build_stk_order(symbol: &str, side: Action, quantity: f64, limit: Option<f64>)
    -> (Contract, Order)` — exactly per arch.md (LMT/MKT, TIF Day, Contract::stock).
  - `pub fn shape_order_ack(order_id: i32, status: &str, symbol: &str, action: &str,
    quantity: f64, limit_price: Option<f64>) -> Value` — exact 6-key object.
  - `pub fn require_live_write_gate(cfg: &Config) -> Result<(), AppError>` — effective-port
    rule (`cfg.port == LIVE_PORT` && env `OMI_ALLOW_LIVE` != "1" ⇒ `AppError::config`), runs
    BEFORE `super::connect`.
  - `pub fn buy(cfg, args)` / `pub fn sell(cfg, args)`: validation (quantity > 0, limit > 0
    when present ⇒ else `AppError::usage`) → gate → connect → `next_valid_order_id()` →
    `build_stk_order` → `place_order(id, &contract, &order)` → bounded first-ack loop
    (`timeout_iter_data(TAKE_FIRST_TIMEOUT)` + Instant-classified None, ADR 0016 pattern):
    first `PlaceOrder::OrderStatus` (Debug-render `.status`) or `PlaceOrder::OpenOrder`
    (Debug-render `.order_state.status`) is the ack; skip ExecutionData/CommissionReport;
    timeout/instant-end ⇒ `AppError::timeout` naming the ALLOCATED order id + "may have been
    SUBMITTED — verify with \`omi orders\`; do NOT retry blindly" (context "buy"/"sell").
    Success ⇒ `shape_order_ack`.
  - `pub fn cancel(cfg, args)`: gate → connect → `cancel_order(order_id, "")` → bounded first
    `CancelOrder::OrderStatus` ⇒ `json!({"order_id", "status"})`; timeout ⇒ same UNKNOWN
    posture (context "cancel").
  - NO retry logic anywhere. NO write calls outside this file.
- `src/ib/mod.rs`: `mod trade;` +
  `pub use trade::{buy, sell, cancel, build_stk_order, shape_order_ack, require_live_write_gate};`
- `src/main.rs`: three dispatch arms.
- `AGENTS.md` + `CLAUDE.md`: replace the read-only red-line bullet with arch.md §Docs
  amendment VERBATIM — no other change to those files.

## Hard constraints

- **Freeze gate**: `git diff 3692c71 <tip> -- tests/stk_orders_command.rs` EMPTY. Never touch
  `tests/`; never repo-wide `cargo fmt` (fmt `src/**` only or skip — AGENTS/CLAUDE.md edits
  are markdown, fmt-irrelevant).
- **Containment**: `place_order`/`submit_order`/`encode_place_order`/`cancel_order` appear in
  `src/ib/trade.rs` ONLY (review greps).
- Validation BEFORE gate BEFORE connect (frozen tests depend on this ordering:
  usage < config < connection).
- Reuse `TAKE_FIRST_TIMEOUT` — no new consts, no new error codes.
- Reads byte-identical; existing frozen suite untouched green.
- Public repo: no account ids/tokens/balances.
- `verify` card-scoped; before PR: full `cargo test` + clippy `-D warnings` clean.

## Freeze coverage

- **Frozen (`tests/stk_orders_command.rs`, 16 tests, offline)**: build_stk_order LMT/MKT ×
  sides (ibapi field asserts); shape_order_ack 6-key + MKT null; the gate matrix (3 verbs ×
  no-env ⇒ config; --port 4001 no-env ⇒ config; env+live+dead-port ⇒ connection; paper
  dead-port ⇒ connection); validation usage errors; help surface.
- **Review must READ (polarity flip)**: gateway fns (bounded loops, UNKNOWN-state message,
  no-retry); containment grep; docs amendment verbatim-match vs arch.md; ack-event choice.
- **Live (operator, PAPER `:4002`, PRD criterion 11, merge gate)**: far-LMT buy → `orders`
  shows working → `cancel` → `completed-orders` Cancelled → `positions` unchanged.
  Operator must log the gateway into the PAPER account for this.

## Round 2 (attempts=1) — local order-id allocator (ADR 0018; paper-wedge routing)

Round 1 implemented ADR 0017 verbatim; PAPER acceptance hung in `next_valid_order_id()`
(unbounded server round-trip inside the crate; gateway never answers RequestIds; NO order was
placed — verified). Fix ONLY the allocation line in `src/ib/trade.rs` per arch.md §Amendment:
`client.next_valid_order_id()?` → `client.next_order_id()`. Same branch `feat/stk-orders`,
same PR #15, same frozen spec (freeze gate vs 3692c71 must stay EMPTY). All other constraints
stand.
