---
id: "01"
title: "option-combo — omi option-combo: multi-leg BAG, leg DSL, net-limit LMT/DAY (ADR 0021)"
status: todo
attempts: 0
verify:
  - "cargo build"
  - "cargo test --test option_combo_command"
spec-paths:
  - "tests/option_combo_command.rs"
impl-paths:
  - "src/cli.rs"
  - "src/ib/trade.rs"
  - "src/ib/mod.rs"
  - "src/main.rs"
  - "AGENTS.md"
  - "CLAUDE.md"
spec-rev: "c0e72a36796e850a121a4b4411d7d0ae6a977a3d"
---

# Card 01 — option-combo (Phase 2 step 4: multi-leg BAG write path)

Make `tests/option_combo_command.rs` green: `omi option-combo` per **ADR 0021**. Read
PRD.md + arch.md + ADR 0021 + CONTEXT.md first — arch.md §Component design + §CLI +
§Docs amendment are the verbatim implementation. HIGH-STAKES write card; machinery is
ADR 0017/0018/0020 VERBATIM REUSE (place_core, gates, allocator, bounded ack, no retry).

## Scope (impl-paths only — do NOT touch tests/)

- src/cli.rs: OptionCombo(OptionComboArgs) — struct VERBATIM from arch §CLI (repeated
  --leg required Vec<String>; --limit plain f64 with allow_hyphen_values — negative =
  credit).
- src/ib/trade.rs: LegSpec (Debug+PartialEq, 6 pub fields) + parse_combo_leg (6-token DSL,
  normalization per spec tests, Err(String) reasons) + build_combo_order
  (Contract::spread().add_leg(conid, LegAction per spec.action).ratio(..).done() in input
  order → .on_exchange().in_currency() → .build()? → back-fill contract.symbol =
  underlying; Order LMT/Some(limit SIGN-FREE)/Day/qty) + shape_combo_order_ack (7 top keys
  + 7-key leg echoes) + gateway fn option_combo: validation (action BUY|SELL ci; legs
  2..=4; per-leg parse naming "leg N: reason"; same-symbol rule; qty finite whole >=1;
  limit finite ANY SIGN) → require_live_write_gate → connect → per-leg contract_details
  conid resolve IN ORDER fail-fast (Contract::call/put builder, first-row conid, empty ⇒
  not_found naming leg N) → next_order_id() → build_combo_order → place_core with the
  combo ack closure; context "option-combo".
- src/ib/mod.rs re-exports; src/main.rs one arm.
- AGENTS.md + CLAUDE.md: arch §Docs amendment VERBATIM (two-text rule); MUST verify
  wc -c CLAUDE.md < 900 before PR (frozen budget, tests/claude_md.rs).

## Hard constraints

- Freeze gate: git diff c0e72a3 <tip> -- tests/option_combo_command.rs EMPTY; ALL
  tests/ frozen (stk + option-orders suites are the regression net for place_core).
- Write calls ONLY in trade.rs; contract_details is a READ call (no polarity trip); no
  MKT arm; no retry; no new consts/error codes/deps; no repo-wide fmt.
- Validation < gate < connect ordering frozen; leg errors name the 1-based index.
- Public repo: no account ids/tokens.
- verify card-scoped; before PR: FULL cargo test + clippy -D warnings green.

## Freeze coverage

- Frozen (tests/option_combo_command.rs, 21 tests, offline): parse_combo_leg happy×2 +
  10-case malformed matrix; build_combo_order (Spread/back-fill/combo_legs order+ratio/
  negative limit/LMT/Day); ack 7+7 keys; gate matrix (live-no-env, port-4001, paper-dead);
  validation matrix (1 leg, 5 legs, mixed underlyings, leg-index naming, bad action,
  qty 1.5, limit NaN, usage-precedes-gate); help. DELIBERATE OMISSION: no env+live+dead
  gate-pass test (live-order hazard; stk twin covers the shared gate).
- Review must READ: gateway fn (per-leg fail-fast resolve, SpreadBuilder usage, symbol
  back-fill), place_core untouched-or-byte-identical for existing verbs, docs two-text +
  <900B, no-MKT/no-retry.
- Live (operator, paper :4002, PRD criteria 1-2): far-net-debit vertical → orders (BAG) →
  cancel → completed-orders Cancelled → positions unchanged. SMART BAG quirks journaled.
