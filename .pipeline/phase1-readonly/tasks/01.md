---
id: "01"
title: "Core: crate scaffold + CLI skeleton + health + config + error/output envelope"
status: todo
attempts: 1
verify:
  - "cargo build"
  - "cargo test --test cli_contract"
spec-paths:
  - "tests/cli_contract.rs"
impl-paths:
  - "Cargo.toml"
  - "src/main.rs"
  - "src/lib.rs"
  - "src/cli.rs"
  - "src/config.rs"
  - "src/error.rs"
  - "src/output.rs"
  - "src/model.rs"
  - "src/ib/mod.rs"
  - "src/ib/client.rs"
spec-rev: "13e522dc70a432b0403cd75d4b5b82531a77a6fa"
---

# Card 01 — core CLI + connection-error envelope

Make `tests/cli_contract.rs` green. Build the `oh-my-ib` crate: library `oh_my_ib` + binary `omi`.

## Scope
- `Cargo.toml`: package `oh-my-ib`; `[lib] name = "oh_my_ib"`; `[[bin]] name = "omi", path = "src/main.rs"`.
  deps: `ibapi = { version = "3.1", default-features = false, features = ["sync"] }`, `clap` (derive),
  `serde` (derive), `serde_json`, `anyhow`, `toml`.
  dev-deps: `assert_cmd`, `predicates`, `serde_json` (the frozen test parses stderr JSON).
- `src/cli.rs`: clap derive. Top-level `Cli` with GLOBAL args (`#[arg(global = true)]`): `--format
  json|table` (default json), `--host` (default 127.0.0.1), `--port` (default 4002), `--live` (sets port
  4001), `--client-id` (default 100), `--account`, `--md-type live|delayed|frozen` (default delayed),
  `--timeout`. `Command` enum with all SEVEN variants present (health + the six from card 02 — register
  them as stubs here is fine, card 02 fills their logic). `--help` must list all seven; `--version` works.
- `src/config.rs`: `Config` + `load(path)` (toml) + `merge(flags)`, precedence flag>toml>default.
- `src/error.rs`: `AppError` (Connection/NotFound/Data/Config + catch-all) → stable `code` + exit code
  (2/3/4/5/1); `ErrorEnvelope{code,message,context}` serialized as `{"error":{...}}`.
- `src/output.rs`: `Format`, `emit_success(Value)` → stdout, `emit_error(&AppError)` → stderr + exit.
- `src/model.rs`: domain structs (start with what health needs; card 02 extends).
- `src/ib/{mod,client}.rs`: `connect(&Config) -> Result<Client, AppError>` over
  `ibapi::client::blocking::Client::connect("host:port", client_id)`; map errors → `AppError::Connection`.
- `src/main.rs`: parse → load+merge config → dispatch `health` (connect + report status/server
  version/accounts) → emit; on `Err` emit error envelope + non-zero exit.
- Add `#[cfg(test)] mod tests` (impl-paths) for config precedence + error-envelope shape (white-box).

## Freeze coverage
Frozen (tests/cli_contract.rs): subcommand presence in `--help`, `--version`, `health --help`, unknown-
subcommand failure, and the connection-error JSON envelope (code="connection") against dead port 65000.
NOT frozen — review must READ: live `health` field correctness (needs a gateway), config precedence,
error mapping. Greenfield: RED here = no crate; goes green when this card creates Cargo.toml + src.

## Review rejection 01 — 2026-06-28
- `src/cli.rs` / `src/config.rs`: `--md-type` was implemented only as a `quote`-local flag, but this card and the PRD require it as a GLOBAL flag with flag>toml>default precedence. Repro on PR head: `omi --md-type delayed quote AAPL --port 65000 --format json` fails with clap `unexpected argument '--md-type' found`.
- Required retry: add `md_type: Option<String>` to `GlobalOpts` with `global = true`, merge it into `Config::md_type`, and make quote-local vs global precedence deterministic.
