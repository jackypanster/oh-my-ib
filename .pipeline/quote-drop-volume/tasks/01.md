---
id: "01"
title: "quote drops volume/size ticks (price ticks only)"
status: done
attempts: 0
verify:
  - "cargo build"
  - "cargo test --test quote_ticks"
spec-paths:
  - "tests/quote_ticks.rs"
impl-paths:
  - "src/ib/quote.rs"
  - "src/ib/mod.rs"
  - "Cargo.toml"
spec-rev: "cd191e2fcce91eba831019423321a4fed0b9509c"
---

# Card 01 — quote = price ticks only

Make `tests/quote_ticks.rs` green and stop `quote` emitting the unreliable volume/size ticks.

## Scope
- `src/ib/quote.rs`:
  - `pub fn quote_price_tick(tick: &TickTypes) -> Option<(String, f64)>` →
    `match tick { TickTypes::Price(p) => Some((format!("{:?}", p.tick_type), p.price)), _ => None }`.
  - Loop: `if matches!(tick, TickTypes::SnapshotEnd) { break }`, then insert only
    `quote_price_tick(&tick)` results. Remove the `TickTypes::Size(_)` arm.
- `src/ib/mod.rs`: `pub use quote::quote_price_tick;`.
- `Cargo.toml`: add `ibapi` to `[dev-dependencies]` (same version/features as the normal dep) so the
  frozen test can construct `TickTypes`. (Cargo.toml is impl-owned, not spec.)

## Freeze coverage
Frozen (`tests/quote_ticks.rs`): `quote_price_tick` Some for a Price tick, None for a Size/volume tick.
NOT frozen — reviewed + live acceptance: `omi --live quote AAPL --md-type delayed` returns price keys
and NO `*Volume`/size key, valid JSON with `symbol`/`delayed`/`ticks`.
